<%= render 'sidebar' %>
<%= javascript_pack_tag "calendar" %>

<div class="flex flex-col justify-center items-center h-full">
  <div id="calendar"></div>
  <div class="w-auto h-1/2 bg-white border border-gray-300 rounded-lg shadow-md flex flex-col justify-center items-center transition-transform duration-100 ease-in-out" id="swipe-widget">
    <h2 class="m-4 no-select text-lg font-semibold text-center"><%= User.current.get_count_good_days %></h2>
  </div>
</div>
